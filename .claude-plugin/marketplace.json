{
  "name": "kodiag",
  "id": "kodiag",
  "owner": { "name": "AtillaErsezen" },
  "metadata": {
    "description": "Trace Python runtime call flow, arguments, return values, errors, and timing via a decorator, then read the generated trace to debug what actually happened.",
    "version": "1.0.1"
  },
  "plugins": [
    {
      "name": "kodiag",
      "source": "./",
      "description": "Trace Python runtime call flow, arguments, return values, errors, and timing via a decorator, then read the generated trace to debug what actually happened.",
      "version": "1.0.1",
      "author": { "name": "AtillaErsezen" },
      "keywords": ["python", "debugging", "tracing", "runtime"],
      "category": "debugging"
    }
  ]
}
